

&НаКлиенте
Процедура ПереключениеЗадачПоИсполнителю(Команда)
	ПереключениеЗадачПоИсполнителюНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПереключениеЗадачПоИсполнителюНаСервере()
	Если Список.ОсновнаяТаблица = "Задача.ЗадачаИсполнителя" Тогда
		Список.ОсновнаяТаблица = "Задача.ЗадачаИсполнителя.ЗадачиПоИсполнителю"
	Иначе
		Список.ОсновнаяТаблица = "Задача.ЗадачаИсполнителя"
	КонецЕсли;
КонецПроцедуры
