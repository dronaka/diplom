Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	Док = Документы.ПланРеагирования.СоздатьДокумент();
	Док.СсылкаОперативнаяСводка = Ссылка;
	Док.Дата = Дата;
	Док.СсылкаСобытие = СсылкаСобытие;
	Док.Записать();

КонецПроцедуры