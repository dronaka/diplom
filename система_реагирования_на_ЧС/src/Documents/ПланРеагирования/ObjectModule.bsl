Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	Док = Документы.СписаниеРесурсов.СоздатьДокумент();
	Выборка = Ресурсы.Выгрузить();
	Док.СписокРесурсов.Загрузить(Выборка);
	Док.Дата = ТекущаяДата();
	Док.Записать(РежимЗаписиДокумента.Проведение);
КонецПроцедуры